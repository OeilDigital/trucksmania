{% extends 'basetruck.html.twig' %}

{% block body %}

<div class="container">
    <div class="row">
        <h3 class="mt-5 mb-5">Formulaire d'enregistrement</h3>
        {% for message in app.flashes('notice') %}
            <div class="alert alert-success">
                {{ message }}
            </div>


        {% endfor %}
        <div class="col-md-6 offset-md-3">
            {{ form_start(form) }}
                <div class="form-group">
                {{ name_truck }} 
                {{ form_widget(form.name_truck,{'attr':{'class':'form-control'}}) }}
                </div>
                <div class="form-group">
                {{ style }} 
                {{ form_widget(form.style,{'attr':{'class':'form-control'}}) }}
                </div>
                <div class="form-group">
                {{ last_name }}
                {{ form_widget(form.last_name,{'attr':{'class':'form-control'}}) }}
                </div>
                <div class="form-group">
                {{ first_name }}
                {{ form_widget(form.first_name,{'attr':{'class':'form-control'}}) }}
                </div>
                {# <div class="form-group">
                {{ password }}
                {{ form_widget(form.password,{'attr':{'class':'form-control'}}) }}
                </div>
                <div class="form-group">
                {{ email }}
                {{ form_widget(form.email,{'attr':{'class':'form-control'}}) }}
                </div> #}
                <div class="form-group">
                {{ phone_number }}
                {{ form_widget(form.phone_number,{'attr':{'class':'form-control'}}) }}
                </div>
                <div class="form-group">
                {{ siret }}
                {{ form_widget(form.siret,{'attr':{'class':'form-control'}}) }}
                </div>
                <div class="form-group">
                {{ picture }}
                {{ form_widget(form.picture,{'attr':{'class':'form-control'}}) }}
                <br/>
                    {% for picture in pictArray %}
                    <div>
                        <img src="{{ asset('/uploads/' ~ picture.name) }}" alt="Image" width="150">
                        {# <a href="{{ relative_path('truck_delete_picture', {'id': picture.id }) }}"
                        data-delete data-token="{{ csrf_token('delete' ~ picture.id) }}">Supprimer</a> #}
                    </div>
                    {% endfor %}
                </div>
                <br>
                {# <div class="form-group">
                <h5>Veuillez confirmer votre email</h5>
                {{ email }}
                {{ form_widget(form.email,{'attr':{'class':'form-control'}}) }}
                </div> #}
                <div class="form-group">
                <button class="btn btn-success mt-5 mb-5">Mettre à jour</button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/pictures.js') }}"></script>
{% endblock %}